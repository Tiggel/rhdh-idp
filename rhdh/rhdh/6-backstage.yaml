---
apiVersion: rhdh.redhat.com/v1alpha3
kind: Backstage
metadata:
  name: developer-hub
  namespace: demo-project
  labels:
    app.kubernetes.io/name: backstage
spec:
  database:
    enableLocalDb: true
  application:
    appConfig:
      configMaps:
        - name: app-config-rhdh
      mountPath: /opt/app-root/src
    extraEnvs:
      envs:
        - name: NODE_TLS_REJECT_UNAUTHORIZED
          value: "0"
      secrets:
        - name: rhdh-secrets
        - name: rhdh-gitlab-secret
    extraFiles:
      configMaps:
        - name: java-service-template
      mountPath: /opt/app-root/src/templates
    replicas: 1
    route:
      enabled: true
      host: rhdh.apps.cluster-4248n.dynamic.redhatworkshops.io
      subdomain: apps.cluster-4248n.dynamic.redhatworkshops.io
